import React from 'react';
import { AutoComplete as KendoAutoComplete } from '@progress/kendo-react-dropdowns';

const AutoComplete = (props) => {
  return (
    <KendoAutoComplete {...props} />
  )
}

export default AutoComplete;
